{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "project-runtime": {
      "type": "stdio",
      "command": "python3",
      "args": ["${PLUGIN_ROOT}/runtime/mcp_server.py"],
      "env": {
        "PYTHONPATH": "${PLUGIN_ROOT}/runtime"
      },
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
